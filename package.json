{
  "name": "openchess",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "dev:server": "bun run --hot packages/server/src/index.ts",
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "build:cli": "bun run --cwd packages/cli build",
    "db:generate": "cd packages/database && bunx prisma generate",
    "db:migrate": "cd packages/database && bunx prisma migrate dev",
    "db:migrate:deploy": "cd packages/database && bunx prisma migrate deploy",
    "db:migrate:status": "cd packages/database && bunx prisma migrate status",
    "db:seed": "bun run packages/server/src/scripts/seed.ts",
    "db:import-puzzles": "bun run packages/server/src/scripts/import-puzzles.ts",
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/database/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json && tsc --noEmit -p packages/server/tsconfig.json",
    "inngest": "inngest-cli dev -u http://localhost:3000/api/inngest"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@types/bun": "^1.3.14",
    "inngest-cli": "^1.40.0",
    "typescript": "^7.0.2"
  },
  "trustedDependencies": [
    "inngest-cli"
  ]
}

{
  "name": "openchess",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "bun run --watch packages/cli/src/index.tsx",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p packages/cli/tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}

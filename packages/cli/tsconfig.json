{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "@opentui/react",
    // `--version` reads the version out of package.json rather than repeating it.
    "resolveJsonModule": true
  }
}
